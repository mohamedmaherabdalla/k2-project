#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT/backend"
if [ "${RELOAD:-0}" = "1" ]; then
  exec "$ROOT/.venv/bin/python" -m uvicorn main:app --reload --port "${PORT:-8000}"
else
  exec "$ROOT/.venv/bin/python" -m uvicorn main:app --port "${PORT:-8000}"
fi
